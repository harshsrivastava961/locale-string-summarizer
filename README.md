# locale-string-summarizer
A small utility or hook that takes a string (long sentence or paragraph) and returns a concise summary or key points tailored for the detected language/locale.  Features:  Detects the user's or document's locale automatically.  Summarizes or extracts key points, respecting grammar/idioms for different languages.
